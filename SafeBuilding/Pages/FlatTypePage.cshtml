@page
@model SafeBuilding.Pages.FlatTypeCrud.IndexModel
@{
    ViewData["Title"] = "FLat Type page";
}
<p>
    <a asp-page="./FlatTypeCrud/Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FlatType[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FlatType[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FlatType[0].Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FlatType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-page="./FlatTypeCrud/Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./FlatTypeCrud/Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
